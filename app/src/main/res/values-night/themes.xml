<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PeakViewer" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Status bar color and size -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/ic_launcher_background</item>
        <item name="android:actionBarSize">36dp</item>
    </style>
</resources>